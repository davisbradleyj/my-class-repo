// declare array
// create second array (empty)
// create for loop to iterate through array
// for array[i]*

var arr1 = [1, 2, 3]
var arr2 = []
for (var i = 0; i < arr1.length; i++) {
    arr2.push(arr1[i] * 2)
    return arr2
}
