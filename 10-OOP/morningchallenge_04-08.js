// establish array and element
var array = [];
var element;

// establish function to determine if element is in array
function find() {
    for (var i=0; i<array.length;i++) {
        // check array to see if it includes element
        if (array[i] == element) {
            // if yes, true
            return true
        } else
        // else false
        return false
    }
}
// is this a built in array method?  do I misunderstand something?
var incl = funct (arr, element) {
    for (var i=0; i<array.length;i++) {
        // check array to see if it includes element
        if (array[i] == element) {
            // if yes, true
            return true
        }
     }
      // else false
        return false
}