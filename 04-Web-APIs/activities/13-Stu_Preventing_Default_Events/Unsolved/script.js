var totalInput = document.querySelector("#total");
var tipAmount = document.querySelector("#tip-amount");
var newAmount = document.querySelector("#new-amount");
var submitEl = document.querySelector("#submit");


submitEl.addEventListener("click", function(totalInput) {
    tipAmount = totalInput.textContent * 0.18
    newAmount = totalInput + tipAmount 

    return shit 
    
//     console.log(event);
  
//     var response = "Thank you for your submission " + nameInput.value + "! We will reach out to you at " + emailInput.value + ".";
//     submissionResponseEl.textContent = response;
//   });




// Name variables

// create some action to call on the input

// create action that multiplies input by 0.18

// create action that adds tip and total

